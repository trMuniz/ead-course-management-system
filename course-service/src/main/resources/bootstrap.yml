spring:
  application:
    name: course-service
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/
    fetch-registry: true
    register-with-eureka: true