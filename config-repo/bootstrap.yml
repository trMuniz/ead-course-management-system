server:
  port: 8888

spring:
  cloud:
    config:
      server:
        native:
          search-locations: file:///Users/muniz/Downloads/config-repo

security:
  basic:
    enabled: false

logging:
  level:
    root: DEBUG
    org.springframework.web: DEBUG
    org.springframework.boot: DEBUG
    org.springframework.cloud: DEBUG